# Antigravity Feature Prompt Template

Implement **one feature only**: `<Feature ID — Feature Name>`.

Before changing anything:

1. Read `AGENTS.md`.
2. Read the project state, tranche plan, feature ledger, and approved feature specification.
3. Inspect the existing repository and relevant live Supabase development state through MCP.

Rules:

- Work only inside the active tranche.
- Do not start another feature.
- Do not create a second web app, admin app, mobile app, database, or duplicate API.
- Preserve completed working features.
- Use shared contracts and domain rules.
- Use Supabase MCP for live backend work and commit matching migrations.
- Use real integrations and honest blocked states; no simulation.
- Run all applicable gates.
- Update durable project documentation.
- Stop after the acceptance report.

Requested outcome:

<Describe the feature clearly>

Acceptance criteria:

<Insert approved criteria>
